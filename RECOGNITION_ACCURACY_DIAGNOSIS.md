# 识别准确率极低诊断报告

**生成时间：** 2026-05-26  
**当前准确率：** 77.5% (86/111)  
**目标准确率：** >90%  
**诊断结论：** 采集数据一致性极差，模型无法学习

---

## 1. 问题现象

### 识别混淆
- 系统识别出了未采集的指令（666、888、yeah）
- 这表明模型过度拟合或特征提取存在系统性偏差

### 采集数据统计

| 指令 | 采集次数 | 平均相似度 | 相似度范围 | 能量变异系数 |
|------|--------|----------|----------|-----------|
| snap | 9 | 0.181 | [0.068, 0.329] | 0.509 |
| fix | 12 | 0.021 | [-0.591, 0.796] | 0.153 |
| twice | 10 | 0.090 | [0.013, 0.954] | 0.055 |
| rock | 8 | 0.093 | [0.025, 0.266] | 0.033 |

---

## 2. 根本原因分析

### 原因1：采集一致性极差（最严重）

**问题描述：**
- 同一指令的不同采集相似度仅0.02-0.18
- 标准参考：同一指令应该>0.7的相似度
- 这意味着采集之间的差异比不同指令的差异还大

**具体表现：**
- **snap指令**：采集间相似度只有18%，能量变异系数50.9%
  - 采集1 vs 采集2: 0.068（差异极大）
  - 采集3 vs 采集4: 0.329（仍然很低）
  - 能量范围：71K-375K（相差5倍）

- **fix指令**：相似度甚至出现负值（-0.591）
  - 说明某些采集的波形方向完全相反
  - 这通常表示肌肉收缩方向或强度差异极大

### 原因2：三通道不均衡

从采集数据统计：
- **Ch1**：标差 23-37（主要信号）
- **Ch2**：标差 3-12（弱信号）
- **Ch3**：标差 0.1-0.2（几乎无信号）

Ch3通道几乎无用，系统实际上只用两个通道工作。

### 原因3：自适应裁剪置信度波动大

从日志分析：
- 裁剪置信度范围：39.6%-97.2%
- 许多采集的裁剪质量为"poor"（39.4-49.1/100）
- 这表示采集的有效信号段位置和长度差异很大

### 原因4：用户采集行为不规范

**推测原因：**
- 每次默念时的肌肉收缩强度不一致
- 肌肉收缩的时间长度差异大（导致自适应裁剪结果差异大）
- 电极位置或压力可能有变化
- 用户的放松状态不一致

---

## 3. 为什么识别准确率是77.5%而不是更低

### 识别算法的鲁棒性

当前识别使用：
1. **多通道融合** - 对不同指令的能量差异敏感
2. **平均相似度** - 与所有采集样本比对，取平均值
3. **自适应阈值** - 动态调整识别阈值

这些机制虽然无法解决采集一致性问题，但提供了基本的区分能力：
- 不同指令的能量水平差异较大（rock最低~300K，fix最高~550K）
- 即使采集内部不一致，指令间的能量差异仍能提供一定的识别信息

### 77.5%的含义

- 系统基本能区分不同指令的能量水平
- 但无法准确识别相似能量的指令
- 识别的25%错误主要来自：
  - 低能量指令识别为高能量指令
  - 相似指令的混淆

---

## 4. 解决方案

### 短期方案（立即可实施）

#### 4.1 改进采集质量控制

**实施步骤：**

1. **采集前校准**
   - 要求用户在采集前做标准化的肌肉收缩练习
   - 显示目标能量范围，用户调整到目标范围内
   - 采集前显示实时能量反馈

2. **采集过程监控**
   - 显示实时波形和能量
   - 如果能量波动过大，提示用户调整
   - 采集完成后显示质量评分

3. **采集后筛选**
   - 计算采集间的相似度
   - 如果相似度<0.5，标记为"质量差"
   - 提示用户删除或重新采集

#### 4.2 改进特征提取

当前问题：
- 特征提取对采集一致性的依赖太强
- 需要更鲁棒的特征

**改进方向：**

1. **使用动态时间规整（DTW）**
   - 已有实现但未充分利用
   - 对采集长度差异更容忍

2. **增加鲁棒特征**
   - 能量比例（而不是绝对能量）
   - 频谱特征（对幅度变化不敏感）
   - 时间特征（峰值位置、上升时间等）

3. **使用模板匹配**
   - 为每个指令生成多个模板（聚类）
   - 识别时与最近的模板比对

#### 4.3 改进识别算法

**当前问题：**
- 使用平均相似度，但采集质量差异大
- 应该给高质量采集更高的权重

**改进方案：**

```typescript
// 加权相似度计算
let totalWeightedSimilarity = 0;
let totalWeight = 0;

for (const collection of cmd.collections) {
  const similarity = calculateFeatureSimilarity(testFeatures, refFeatures);
  
  // 基于采集质量的权重
  const quality = collection.qualityScore || 50;  // 0-100
  const weight = quality / 100;
  
  totalWeightedSimilarity += similarity * weight;
  totalWeight += weight;
}

const weightedAverage = totalWeightedSimilarity / totalWeight;
```

### 中期方案（1-2周内）

#### 4.4 实现采集质量评分系统

**评分维度：**

1. **一致性评分**
   - 与其他采集的平均相似度
   - 目标：>0.6

2. **能量稳定性评分**
   - 能量变异系数
   - 目标：<0.2

3. **信号质量评分**
   - SNR（信噪比）
   - 目标：>10dB

4. **综合评分**
   - 加权平均
   - 目标：>70/100

**实施：**
- 采集完成后自动计算评分
- 评分<60时提示用户重新采集
- 保存评分用于识别时加权

#### 4.5 实现采集建议系统

**基于诊断数据提示用户：**

```
采集质量分析：
- snap: 采集一致性差 (相似度18%)
  建议：保持肌肉收缩强度一致，每次采集时间长度相同
  
- fix: 采集方向不一致 (存在反向采集)
  建议：确保电极位置正确，肌肉收缩方向一致
  
- twice: 采集能量波动大 (变异系数5.5%)
  建议：保持稳定的肌肉收缩力度
```

### 长期方案（1个月内）

#### 4.6 实现自适应模型训练

**思路：**
- 不再要求采集一致性
- 使用聚类算法找到最稳定的采集子集
- 只用最稳定的采集训练模型

**实施：**

```typescript
// 聚类采集
const clusters = clusterCollections(cmd.collections, 3);

// 选择最紧凑的聚类
const bestCluster = clusters.reduce((best, current) => {
  const currentCohesion = calculateClusterCohesion(current);
  const bestCohesion = calculateClusterCohesion(best);
  return currentCohesion > bestCohesion ? current : best;
});

// 只用最佳聚类训练
const trainingData = bestCluster.map(col => processWaveform(col.waveform));
```

#### 4.7 实现多模型融合

**使用多个识别模型：**
1. 特征相似度模型（当前）
2. CNN模型（已有实现）
3. DTW模型
4. 模板匹配模型

**融合策略：**
```typescript
const results = [
  featureSimilarityModel.predict(testWaveform),
  cnnModel.predict(testWaveform),
  dtwModel.predict(testWaveform),
  templateModel.predict(testWaveform),
];

// 投票或加权平均
const finalResult = ensembleModels(results);
```

---

## 5. 实施优先级

### 立即实施（今天）
1. ✅ 采集前校准提示
2. ✅ 采集后质量评分
3. ✅ 加权相似度计算

### 本周实施
4. 采集建议系统
5. 改进DTW实现
6. 增加鲁棒特征

### 下周实施
7. 聚类训练数据
8. 多模型融合
9. 完整的质量控制系统

---

## 6. 预期效果

| 方案 | 预期准确率 | 实施时间 |
|------|----------|--------|
| 当前 | 77.5% | - |
| 采集质量控制 | 82-85% | 1天 |
| 加权相似度 | 85-88% | 1天 |
| 聚类训练 | 88-91% | 3天 |
| 多模型融合 | 91-95% | 5天 |

---

## 7. 建议

**最重要的是改进采集流程，而不是识别算法。**

当前的识别算法已经相当复杂，但采集数据的一致性问题无法通过算法完全解决。

**建议优先级：**
1. 改进采集质量控制（最重要）
2. 改进特征提取（中等重要）
3. 改进识别算法（较低重要）

