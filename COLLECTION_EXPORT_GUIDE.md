# 采集数据导出功能使用指南

## 概述

采集数据导出功能提供了一个完整的数据分析工具，用户可以导出采集的肌电数据，包含两次裁剪的所有操作数据，用于深度分析和参数优化。

## 功能特性

### 1. 完整的导出数据

导出的数据包含以下内容：

| 数据类型 | 说明 | 用途 |
|---------|------|------|
| **原始波形** | CH1、CH2、CH3的完整采集数据 | 查看采集质量和噪声水平 |
| **第一次裁剪参数** | 能量检测后的裁剪范围、置信度、峰值数 | 分析能量阈值是否合理 |
| **第二次裁剪参数** | 固定长度裁剪的范围和对齐方式 | 分析固定长度是否合适 |
| **时长统计** | 原始 → 第一次 → 第二次的时长变化 | **关键：判断是否需要调整固定长度** |
| **能量分析** | 阈值、置信度、峰值位置、能量分布 | 评估采集质量 |
| **质量评分** | 综合评分和各因素的详细评分 | 判断采集是否可用 |
| **识别结果** | 识别的指令和置信度（如果已识别） | 验证识别准确性 |

### 2. 导出格式

系统支持多种导出格式：

- **JSON格式** - 完整的原始数据和中间过程，便于程序化分析
- **CSV格式** - 表格格式，便于在Excel中查看和对比
- **ZIP打包** - 将多条采集数据打包成一个文件，便于批量传输

### 3. 导出方式

- **单个导出** - 逐个下载JSON/CSV文件
- **打包导出** - 将所有选中的采集打包成ZIP文件，包含汇总表

## 使用步骤

### 步骤1：完成采集

首先，进行采集训练：

1. 点击首页的"采集训练"按钮
2. 输入指令名称（如"666"、"888"等）
3. 点击"开始采集"，默念指令3秒
4. 点击"停止采集"
5. 重复5-10次
6. 点击"保存"完成采集

### 步骤2：进入数据导出页面

1. 登录系统
2. 点击首页的"📊 数据导出"按钮
3. 进入数据导出管理页面

### 步骤3：选择指令和采集

1. **选择指令** - 从下拉菜单中选择要导出的指令
2. **选择采集** - 勾选要导出的采集数据（支持全选）

### 步骤4：选择导出格式

根据需要选择导出方式：

| 导出方式 | 适用场景 | 说明 |
|---------|---------|------|
| **JSON** | 程序化分析 | 包含完整的原始数据和中间过程 |
| **CSV** | Excel分析 | 表格格式，便于对比多条采集 |
| **ZIP** | 批量传输 | 打包所有选中的采集，包含汇总表 |

### 步骤5：导出数据

点击对应的导出按钮，系统会自动生成并下载文件。

## 导出数据的结构

### JSON文件结构

```json
{
  "exportTime": "2026-05-20T10:30:00.000Z",
  "exportVersion": "2.0",
  "metadata": {
    "commandName": "666",
    "collectionIndex": 0,
    "collectionTimestamp": "2026-05-20T10:25:00.000Z"
  },
  "rawWaveform": {
    "ch1": [...],
    "ch2": [...],
    "ch3": [...],
    "duration": 5120,
    "totalSamples": 2560
  },
  "firstCropping": {
    "algorithm": "energy-detection",
    "parameters": {...},
    "energyAnalysis": {...},
    "peakDetection": {...},
    "result": {
      "startIdx": 130,
      "endIdx": 1820,
      "length": 1690,
      "durationMs": 3380
    }
  },
  "secondCropping": {
    "algorithm": "fixed-length",
    "parameters": {...},
    "result": {
      "startIdx": 364,
      "endIdx": 876,
      "length": 512,
      "durationMs": 1024
    }
  },
  "timingStats": {
    "rawDurationMs": 5120,
    "afterFirstCroppingMs": 3380,
    "afterSecondCroppingMs": 1024,
    "timingAnalysis": {
      "rawToFirstCropRatio": 1.51,
      "firstCropToSecondCropRatio": 3.30,
      "recommendation": "..."
    }
  },
  "qualityScore": {
    "isAcceptable": true,
    "score": 82,
    "factors": {...},
    "issues": [...],
    "recommendations": [...]
  }
}
```

### CSV文件结构

CSV文件包含以下列：

```
指令,采集序号,原始时长(ms),第一次裁剪(ms),第二次裁剪(ms),能量置信度,峰值数,质量评分,是否可接受
666,1,5120,3380,1024,92%,1,82,是
```

### ZIP文件结构

```
collections-export-2026-05-20-10-30.zip
├── metadata.json (导出元数据)
├── summary.csv (所有采集的汇总表)
├── collections/
│   ├── json/
│   │   ├── 666-1.json
│   │   ├── 666-2.json
│   │   └── ...
│   └── csv/
│       ├── 666-1.csv
│       ├── 666-2.csv
│       └── ...
└── analysis-report.md (自动生成的分析报告)
```

## 关键指标解读

### 时长分析

**最重要的指标** - 用于判断是否需要调整`FIXED_WAVEFORM_LENGTH`参数：

- **原始时长** - 采集的完整时长（毫秒）
- **第一次裁剪后** - 能量检测后的时长
- **第二次裁剪后** - 固定长度裁剪后的时长（应该是512采样点 = 1024ms）

**判断标准**：
- 如果第一次裁剪后的时长 > 1024ms，说明能量阈值可能过低，需要调整
- 如果多条采集的第一次裁剪时长差异很大，说明采集质量不稳定

### 质量评分

综合评分（0-100）：

- **80-100** - 优秀，可以直接使用
- **60-79** - 良好，可以使用但需要注意
- **40-59** - 一般，建议重新采集
- **0-39** - 较差，不建议使用

### 能量置信度

表示有效信号的比例（0-1）：

- **> 0.8** - 很好，信号清晰
- **0.5-0.8** - 良好，可以接受
- **< 0.5** - 较差，可能有大量噪声

### 峰值数

表示检测到的峰值个数：

- **1** - 理想情况，单音节指令
- **> 1** - 多音节指令或采集中有噪声

## 数据分析建议

### 场景1：判断是否需要调整固定长度

1. 导出5-10条采集数据
2. 查看**第一次裁剪后的时长**
3. 如果大部分采集都 > 1024ms，考虑：
   - 降低能量阈值（使裁剪更紧凑）
   - 或增加`FIXED_WAVEFORM_LENGTH`参数

### 场景2：诊断识别准确率低

1. 导出识别错误的采集数据
2. 查看**质量评分**和**能量置信度**
3. 比较正确识别和错误识别的采集特征
4. 分析是否是采集质量问题或算法问题

### 场景3：对比不同指令的特征

1. 导出多个指令的采集数据
2. 对比**时长分布**、**能量分布**、**峰值数**
3. 识别指令之间的差异
4. 优化裁剪和识别算法

## 常见问题

### Q1: 导出的时长为什么这么长？

**A:** 这是正常的。系统会自动检测有效信号的范围，然后进行两次裁剪：
1. 第一次：能量检测裁剪（去除前后的空白）
2. 第二次：固定长度裁剪（标准化为512采样点）

如果第一次裁剪后的时长仍然很长，说明采集的发音时间较长，可能需要调整采集方式或能量阈值。

### Q2: 质量评分低是什么原因？

**A:** 质量评分低可能是以下原因：
- 能量置信度低 - 采集中有大量噪声
- 峰值质量低 - 检测到多个峰值（多音节或噪声）
- 长度适配度低 - 采集时间过长或过短
- 噪声水平高 - 电极接触不良或环境干扰

### Q3: 能否只导出某些采集？

**A:** 可以。在选择采集时，勾选要导出的采集序号，支持全选或单选。

### Q4: 导出的数据可以用来做什么？

**A:** 导出的数据可以用于：
- 深度分析采集质量
- 优化裁剪参数
- 诊断识别错误
- 生成统计报告
- 与开发者共享进行技术支持

## 技术细节

### 采样率

所有数据的采样率为 **500Hz**，即每秒500个采样点。

### 固定长度

目前固定长度设置为 **512采样点**，对应 **1024毫秒**（约1秒）。

### 能量阈值

能量阈值默认为 **0.35**（归一化值），可根据实际情况调整。

### 对齐方式

固定长度裁剪使用 **中心对齐**，即从有效信号的中心向两侧各取256个采样点。

## 反馈和改进

如果在使用过程中发现任何问题或有改进建议，请：

1. 记录具体的问题描述
2. 导出相关的采集数据
3. 提供反馈给开发者

这样可以帮助我们更好地改进系统。

## 相关链接

- [采集训练指南](./COLLECTION_TRAINING_GUIDE.md)
- [默念测试指南](./RECOGNITION_TEST_GUIDE.md)
- [系统设置指南](./SETTINGS_GUIDE.md)
