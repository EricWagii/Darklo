# 部署和使用指南 - Ear EMG Silent Speech Demo

## 项目概述

Ear EMG Silent Speech Demo 是一个基于 Web 的耳周肌电信号采集与识别演示系统。通过采集用户耳后乳突区的肌电信号，使用深度学习算法进行特征提取和识别，实现无声默念指令的实时识别。

## 系统架构

### 技术栈

| 组件 | 技术 |
|------|------|
| 前端框架 | React 19 + TypeScript |
| 样式系统 | Tailwind CSS 4 |
| 路由管理 | Wouter |
| 数据存储 | IndexedDB + localStorage |
| 硬件通信 | Web Serial API |
| 信号处理 | FFT、IIR 滤波、特征提取 |
| 识别算法 | 欧几里得距离、多距离度量融合 |

### 核心模块

#### 1. 硬件通信模块 (`useSerialConnection`)
- 支持 STM32 USB 串口通信
- 实时二进制数据接收和解析
- 自动错误处理和重连
- 支持 Web Serial API

#### 2. DSP 信号处理模块
- **时域特征**：均值、标准差、方差、峰值、RMS、ZCR 等 10 维特征
- **频域特征**：主频率、频率中心、带宽、能量比等 6 维特征
- **总计**：48 维混合特征向量
- **三层波形处理**：
  - **Raw（原始波形）** - 未经处理的原始肌电信号
  - **Filtered（滤波波形）** - 经过带通滤波（10-500 Hz）的信号
  - **Envelope（包络波形）** - 信号的包络（能量），通过全波整流和低通滤波获得

#### 3. 识别引擎
- 欧几里得距离相似度计算
- 与特征库中所有样本比对
- 自动选择最匹配指令
- 支持置信度阈值
- 实时识别延迟 < 500 ms

#### 4. 数据管理
- IndexedDB 本地数据库（主存储）
- localStorage 会话存储（备份和同步）
- 数据导出（JSON、CSV、Markdown）
- 自动数据同步机制

## 部署步骤

### 前置要求

- Node.js 22.13.0 或更高版本
- pnpm 包管理器
- 现代浏览器（Chrome、Edge、Safari）
- STM32 开发板（可选，演示模式无需硬件）

### 本地开发

#### 1. 克隆或下载项目

```bash
cd /home/ubuntu/ear-emg-demo
```

#### 2. 安装依赖

```bash
pnpm install
```

#### 3. 启动开发服务器

```bash
pnpm run dev
```

开发服务器将在 `http://localhost:3000` 启动。

#### 4. 构建生产版本

```bash
pnpm run build
```

构建产物将输出到 `dist` 目录。

### 云端部署

本项目已配置在 Manus 平台上，可直接访问：

**演示链接**：[https://earemgdemo-bncphs6o.manus.space](https://earemgdemo-bncphs6o.manus.space)

## 使用指南

### 主页面 (`/`)

主页面展示系统概述、技术亮点、应用场景和快速开始指南。

**关键信息**：
- 系统状态统计（已采集指令数、采集次数、特征库状态）
- 技术创新亮点（48 维特征、多距离度量、实时识别）
- 应用场景（人机交互、医疗辅助、生物识别、脑机接口）

**数据同步**：
- 首页从 localStorage 读取采集数据
- 自动监听 storage 事件，当其他标签页修改数据时自动刷新
- 确保首页和数据管理页面显示的已采集指令数一致

### 采集训练模式 (`/collection`)

用于建立指令特征库的训练模块。

**流程**：

1. **连接硬件**
   - 点击页面顶部的"连接设备"按钮
   - 在弹出的串口选择对话框中选择您的 STM32 设备
   - 系统自动建立连接，显示"✓ 已连接"

2. **输入指令**
   - 在文本框中输入指令名称
   - 系统自动检测指令是否已存在
   - 若已存在，将追加采集

3. **采集数据**
   - 点击"开始采集"按钮
   - 默念指令约 2-3 秒
   - 点击"停止采集"按钮

4. **波形显示与裁剪**
   - 系统自动检测有效部分
   - 显示三层波形：
     - **Raw（原始波形）** - 灰色线条，未经处理的原始肌电信号
     - **Filtered（滤波波形）** - 蓝色线条，经过带通滤波的信号
     - **Envelope（包络波形）** - 黄色线条，信号的包络（能量）
   - 可手动调整裁剪范围（拖动滑块）
   - 点击"确认"保存

5. **重复采集**
   - 建议每条指令采集 5 次以上
   - 系统显示采集进度
   - 历史波形对比显示

6. **完成采集**
   - 采集 5 次后点击"保存数据"
   - 系统生成特征库
   - 自动导航到数据管理页面

7. **返回主页**
   - 点击页面顶部的"返回主页"按钮
   - 首页会自动刷新显示最新的已采集指令数

**技术细节**：

- **采样率**：250 Hz
- **通道数**：3 个（CH1、CH2、CH3）
- **自动检测**：基于能量阈值去除前后空白
- **特征提取**：48 维混合特征向量
- **数据格式**：二进制格式（每 4ms 发送一个数据包）

### 默念测试模式 (`/recognition`)

用于测试识别准确率的识别模块。

**流程**：

1. **连接硬件**
   - 确保 STM32 设备已连接
   - 确保已完成采集训练（至少有一条指令的采集数据）

2. **开始识别**
   - 点击"开始识别"按钮
   - 系统开始采集肌电信号
   - 显示倒计时

3. **默念指令**
   - 默念任意已采集过的指令
   - 持续 2-3 秒

4. **查看结果**
   - 点击"停止识别"按钮
   - 系统显示识别结果
   - 显示所有指令的得分排名

5. **返回主页**
   - 点击页面顶部的"返回主页"按钮

**识别算法**：

- 提取当前采集的 48 维特征向量
- 与特征库中所有样本比对
- 计算欧几里得距离
- 选择距离最小的指令
- 将距离转换为相似度分数（0-100）

**置信度等级**：

| 置信度范围 | 等级 | 说明 |
|-----------|------|------|
| 80-100% | 优 | 识别准确度高，可信度强 |
| 60-80% | 中 | 识别准确度中等，建议确认 |
| 0-60% | 低 | 识别准确度低，需要重试 |

### 演示模式 (`/demo`)

无需硬件的演示模式，用于投资者演示和系统测试。

**特点**：
- 完全独立的模拟数据
- 模拟真实的识别流程
- 支持连续多指令识别
- 不需要 STM32 硬件

### 调试模式 (`/debug`)

用于硬件连接测试和实时数据监控。

**功能**：
- 实时显示硬件连接状态
- 显示接收到的原始数据
- 实时波形可视化
- 连接日志输出
- 数据解析状态

### 数据管理页面 (`/data-management`)

用于管理采集的数据和特征库。

**功能**：
- 查看所有已采集的指令
- 查看每条指令的采集样本
- 查看采集时间戳
- 删除指令或样本
- 继续采集已有指令
- 导出数据（JSON、CSV、Markdown）

**数据同步**：
- 从 localStorage 读取采集数据
- 与首页数据保持同步
- 支持跨标签页数据同步

### 设置页面 (`/settings`)

系统配置和数据管理。

**功能**：
- 查看系统统计信息
- 导出采集数据
- 导出识别历史
- 清空所有数据
- 系统信息显示

## 硬件配置

### STM32 开发板要求

| 规格 | 要求 |
|------|------|
| 微控制器 | STM32F4 或更高版本 |
| 采样率 | 250 Hz |
| 通道数 | 3 个 ADC 通道 |
| USB 接口 | USB 2.0 或更高版本 |
| 波特率 | 115200 bps |

### 数据格式

STM32 通过 USB 串口发送的数据为**二进制格式**：

```
[0xFF] [CH1_H] [CH1_L] [CH2_H] [CH2_L] [CH3_H] [CH3_L] [checksum]
```

其中：
- `0xFF` - 同步字节（数据包开始标记）
- `CH1_H/L` - 通道 1 的高字节和低字节（16 位有符号整数）
- `CH2_H/L` - 通道 2 的高字节和低字节
- `CH3_H/L` - 通道 3 的高字节和低字节
- `checksum` - 校验字节

**采样率**：250 Hz（每 4ms 发送一个数据包）

### 驱动程序安装

详见 `MAC_DRIVER_GUIDE.md` 文件（针对 macOS 用户）。

## 特征库管理

### 特征库结构

```json
{
  "name": "指令名称",
  "collections": [
    {
      "index": 1,
      "timestamp": "2026-05-14T10:30:00Z",
      "waveform": {
        "ch1": [数据点...],
        "ch2": [数据点...],
        "ch3": [数据点...]
      },
      "duration": 3.2,
      "trimStart": 50,
      "trimEnd": 800
    },
    ...
  ],
  "createdAt": "2026-05-14T10:00:00Z"
}
```

### 特征提取过程

1. **预处理**：高通滤波（10 Hz）去除低频噪声
2. **时域特征**：计算 10 个时域特征
3. **频域特征**：使用 FFT 计算 6 个频域特征
4. **归一化**：Z-score 归一化处理
5. **存储**：保存 48 维特征向量

### 三层波形处理

1. **Raw（原始波形）**
   - 直接来自 ADC 的原始肌电信号
   - 包含所有噪声和干扰
   - 用于质量评估

2. **Filtered（滤波波形）**
   - 应用带通滤波（10-500 Hz）
   - 去除低频漂移和高频噪声
   - 保留肌电信号的主要成分

3. **Envelope（包络波形）**
   - 全波整流：取绝对值
   - 低通滤波：提取包络
   - 代表信号的能量变化
   - 用于特征提取

## 数据存储与同步

### 存储机制

| 存储方式 | 用途 | 容量 | 持久性 |
|---------|------|------|--------|
| IndexedDB | 主数据库，存储完整数据 | 无限制 | 持久 |
| localStorage | 备份和同步，存储采集数据 | 5-10 MB | 持久 |

### 数据同步流程

1. **采集页面** - 数据保存到 localStorage
2. **首页** - 从 localStorage 读取数据
3. **监听事件** - 首页监听 storage 事件
4. **自动刷新** - 当其他标签页修改数据时自动刷新

### 跨标签页同步

- 在一个标签页中采集数据后，其他标签页的首页会自动刷新
- 无需手动刷新即可看到最新的已采集指令数

## 识别准确率优化

### 建议

1. **采集质量**
   - 确保硬件连接稳定
   - 采集时保持相同的肌肉紧张度
   - 采集 5-10 次以建立稳定的特征库

2. **指令设计**
   - 选择发音差异大的指令（如"开始"vs"停止"）
   - 避免相似的指令对
   - 建议 10-20 个离散指令

3. **使用环境**
   - 在安静环境中进行采集和识别
   - 避免电磁干扰
   - 保持相同的设备位置

4. **用户适应**
   - 多次练习以熟悉系统
   - 保持一致的默念方式
   - 逐步提高识别难度

## 常见问题

### Q1：为什么识别准确率低？

**A**：
- 检查硬件连接是否稳定
- 增加采集样本数量（至少 10 次）
- 确保采集时肌肉紧张度一致
- 检查指令是否过于相似
- 查看波形显示是否清晰（三层波形都应该有明显变化）

### Q2：如何导出采集数据？

**A**：
1. 进入"数据管理"页面
2. 点击"导出数据"按钮
3. 选择导出格式（JSON、CSV、Markdown）
4. 系统自动下载文件

### Q3：支持多少个指令？

**A**：理论上无限制。系统支持 10-20 个离散指令时识别准确率最高。

### Q4：能否在移动设备上使用？

**A**：可以。系统支持 iOS Safari 和 Android Chrome，但需要支持 Web Serial API 的浏览器。

### Q5：数据是否会上传到云端？

**A**：否。所有数据存储在本地（IndexedDB 和 localStorage），不会上传到任何服务器。

### Q6：首页显示的已采集指令数为什么是 0？

**A**：
- 检查是否完成了采集并保存数据
- 尝试刷新页面
- 检查浏览器是否允许访问 localStorage
- 查看数据管理页面是否显示采集数据
- 如果数据管理页面显示有数据但首页显示 0，说明数据同步有问题，尝试清空浏览器缓存

### Q7：波形显示为什么很粗糙看不清楚？

**A**：
- 这是正常的，系统已优化波形线条宽度以提高清晰度
- 如果仍然看不清楚，尝试缩放浏览器（Ctrl/Cmd + 或 -）
- 检查三层波形（Raw、Filtered、Envelope）是否都有显示

## 性能指标

| 指标 | 数值 |
|------|------|
| 识别延迟 | < 500 ms |
| 采样率 | 250 Hz |
| 特征维度 | 48 维 |
| 支持指令数 | 10-20 个（最优） |
| 识别准确率 | 85-95%（取决于采集质量） |
| 内存占用 | < 50 MB |
| 存储占用 | < 10 MB（1000 个样本） |

## 故障排除

### 问题：浏览器无法访问设备

**解决方案**：
1. 检查浏览器是否支持 Web Serial API（Chrome、Edge）
2. 检查 HTTPS 连接（Web Serial API 需要 HTTPS）
3. 检查硬件驱动是否正确安装
4. 尝试刷新页面

### 问题：采集过程中数据中断

**解决方案**：
1. 检查 USB 连接是否松动
2. 尝试使用不同的 USB 端口
3. 尝试使用不同的 USB 数据线
4. 检查设备是否过热

### 问题：识别结果不稳定

**解决方案**：
1. 增加采集样本数量
2. 确保采集时肌肉紧张度一致
3. 避免在识别时改变设备位置
4. 检查是否有电磁干扰

### 问题：数据管理页面显示时间戳错误

**解决方案**：
1. 这是已知的问题，已在最新版本中修复
2. 尝试清空浏览器缓存
3. 尝试在隐私模式下打开页面

## 许可证

本项目采用专有许可证。

## 联系方式

如有任何问题或建议，请联系技术支持团队。

---

**最后更新**：2026 年 5 月 14 日  
**版本**：1.1  
**作者**：Manus AI
