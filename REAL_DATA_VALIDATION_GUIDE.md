# 真实数据识别诊断 - 任务指南

## 📋 任务概述

本阶段目标是通过真实数据验证识别准确率链路，找出准确率低的根本原因。**不做算法改动，只做诊断**。

---

## 🎯 采集和测试计划

### 第一步：准备指令（5 分钟）

选择 **4 个不同的指令**，例如：
- 指令 A：`up`（向上）
- 指令 B：`down`（向下）
- 指令 C：`left`（向左）
- 指令 D：`right`（向右）

> **提示**：选择发音差异明显的指令，避免音似指令（如 "yes" vs "yeah"）

---

### 第二步：采集训练样本（20-30 分钟）

**对每个指令，采集 8-10 条有效样本：**

1. 进入 **"采集训练"** 页面
2. 选择指令名称
3. 点击 **"开始采集"**
4. 等待倒计时完成
5. 默念指令 2-3 秒（清晰、自然、一致）
6. 查看采集结果
   - ✅ 绿色 = 质量良好，保存
   - ⚠️ 黄色 = 质量一般，可考虑重新采集
   - ❌ 红色 = 质量差，必须重新采集

**每个指令的目标**：
- 采集次数：**8-10 条**
- 质量要求：**至少 6 条绿色**
- 波形长度：**全部 512 采样点**

**采集完成后的检查**：
1. 进入 **"数据管理"** 页面
2. 查看每个指令的统计信息
3. 验证：
   - 采集次数 ≥ 8
   - 没有空波形或全零波形
   - 所有波形长度都是 512

---

### 第三步：进行识别测试（20-30 分钟）

**对每个指令，进行 5 次识别测试：**

1. 进入 **"默念测试"** 页面
2. 点击 **"开始识别"**
3. 等待倒计时完成
4. 默念指令 2-3 秒（与采集时保持一致）
5. 查看识别结果
6. **关键步骤**：选择"用户反馈"中的 **"实际默念的指令"**
   - 如果识别正确，选择相同指令
   - 如果识别错误，选择你实际默念的指令
7. 点击 **"提交反馈"**

**测试完成后的检查**：
- 总测试数：4 指令 × 5 次 = **20 次**
- 每个指令的测试数：**5 次**
- 每个测试都有 **"实际默念的指令"** 标记

---

## 📊 数据导出

### 导出三份诊断数据

在 **"默念测试"** 页面的 **"识别历史"** 下方，有两个导出按钮：

#### 1️⃣ **📊 导出诊断数据** （最重要）
- 文件名：`diagnosis-YYYY-MM-DD.json`
- 包含内容：
  - 所有指令的样本统计（数量、波形长度、通道质量）
  - 每次识别试验的详细数据
  - 每个指令的准确率
  - 特征分离度分析
  - 诊断建议

#### 2️⃣ **📋 导出识别结果** （参考用）
- 文件名：`recognition-results-YYYY-MM-DD.json`
- 包含内容：
  - 每次识别的预测结果
  - 实际指令
  - 置信度
  - 所有候选得分

#### 3️⃣ **EMG 原始数据** （如需要）
- 进入 **"数据管理"** 页面
- 选择指令
- 点击 **"导出数据"** 按钮
- 文件名：`emg-data-{commandName}.json`

---

## 📈 诊断报告结构

导出的 `diagnosis-YYYY-MM-DD.json` 包含以下部分：

### 1. commandsSummary（指令统计）
```json
{
  "commandName": "up",
  "collectionCount": 8,
  "waveformLengths": [512, 512, 512, ...],
  "allLengthsAre512": true,
  "ch1": { "rms": 0.45, "peak": 1.23, "variance": 0.18 },
  "ch2": { "rms": 0.52, "peak": 1.45, "variance": 0.21 },
  "ch3": { "rms": 0.38, "peak": 1.10, "variance": 0.15 },
  "abnormalFlags": []
}
```

**关键指标**：
- `collectionCount` ≥ 8：样本数是否充足
- `allLengthsAre512`：波形长度是否一致
- `ch1/ch2/ch3` RMS/Peak/Variance：通道信号质量

### 2. recognitionTrials（识别试验）
```json
{
  "timestamp": 1234567890000,
  "predictedCommand": "up",
  "actualCommand": "up",
  "isCorrect": true,
  "confidence": 85.5,
  "threshold": 60,
  "allScores": [
    { "command": "up", "score": 0.855 },
    { "command": "down", "score": 0.120 },
    ...
  ],
  "top1Score": 0.855,
  "top2Score": 0.120,
  "top1Top2Diff": 0.735,
  "topK": 3,
  "channelWeights": { "ch1": 0.30, "ch2": 0.60, "ch3": 0.10 }
}
```

**关键指标**：
- `isCorrect`：是否识别正确
- `confidence`：置信度（0-100）
- `top1Top2Diff`：top1 和 top2 的分差（>0.5 表示决策明确）
- `allScores`：所有候选的得分

### 3. perCommandAccuracy（每个指令的准确率）
```json
{
  "commandName": "up",
  "totalTests": 5,
  "correctTests": 4,
  "accuracy": 0.80,
  "mostCommonConfusions": [
    { "wrongPrediction": "down", "count": 1 }
  ]
}
```

**关键指标**：
- `accuracy`：准确率（目标 > 0.80）
- `mostCommonConfusions`：常见错误识别

### 4. diagnosis（诊断建议）
```json
{
  "insufficientSamples": [],
  "abnormalSamples": [],
  "thresholdIssue": "...",
  "top1Top2DiffAnalysis": "...",
  "ch2ContributionAnalysis": "...",
  "unknownContamination": "..."
}
```

---

## 🔍 诊断检查清单

完成导出后，请检查以下项目：

### 样本质量
- [ ] 每个指令采集数 ≥ 8
- [ ] 所有波形长度 = 512
- [ ] 没有空波形或全零波形
- [ ] ch1/ch2/ch3 RMS 都 > 0.1（信号强度足够）

### 识别准确率
- [ ] 总准确率 ≥ 70%（诊断阶段的基线）
- [ ] 每个指令准确率 ≥ 60%
- [ ] 没有某个指令准确率特别低（< 40%）

### 识别决策
- [ ] 平均 top1/top2 分差 > 0.5（决策明确）
- [ ] 没有 top1/top2 分差 < 0.1 的情况（容易混淆）
- [ ] 置信度分布合理（不全是 100% 或全是 50%）

### 通道贡献
- [ ] ch2 权重最高（0.60）
- [ ] ch2 的 RMS 通常最高
- [ ] 三个通道的信号质量都可接受

---

## 📝 诊断报告模板

完成测试后，请生成一份诊断报告，包含：

### 1. 总体统计
- 总准确率：__%
- 测试总数：20 次
- 正确识别：__ 次
- 错误识别：__ 次

### 2. 每个指令的准确率
| 指令 | 采集数 | 测试数 | 正确数 | 准确率 | 常见错误 |
|------|--------|--------|--------|--------|----------|
| up   | 8      | 5      | 4      | 80%    | 1× down  |
| down | 8      | 5      | 5      | 100%   | -        |
| left | 8      | 5      | 3      | 60%    | 2× right |
| right| 8      | 5      | 4      | 80%    | 1× left  |

### 3. 错误识别矩阵
```
实际\预测  up   down  left  right
up         4    1     0     0
down       0    5     0     0
left       0    0     3     2
right      0    0     1     4
```

### 4. allScores 样例分析
**正确识别样例**（5 个）：
- 示例 1：up → [0.85, 0.12, 0.02, 0.01]，分差 0.73 ✅
- 示例 2：down → [0.92, 0.05, 0.02, 0.01]，分差 0.87 ✅
- ...

**错误识别样例**（5 个）：
- 示例 1：实际 left，预测 right → [0.48, 0.52, 0.01, 0.01]，分差 -0.04 ❌
- 示例 2：实际 up，预测 down → [0.45, 0.55, 0.01, 0.01]，分差 -0.10 ❌
- ...

### 5. top1/top2 分差统计
- 平均分差：__
- 最小分差：__
- 分差 < 0.1 的情况：__ 次（容易混淆）
- 分差 > 0.5 的情况：__ 次（决策明确）

### 6. 通道贡献分析
- ch1 RMS 平均：__
- ch2 RMS 平均：__（应该最高）
- ch3 RMS 平均：__
- ch2 是否确实是主通道：是/否

### 7. 诊断结论和建议

根据上述数据，给出以下建议：

**问题诊断**：
- [ ] 样本数不足（< 8）
- [ ] 样本质量差（RMS < 0.1 或波形长度不一致）
- [ ] 阈值设置不合理（需要调整）
- [ ] top1/top2 分差过小（指令容易混淆）
- [ ] ch2 不是主通道（权重配置错误）
- [ ] 历史脏数据污染（需要清理）

**改进建议**：
- [ ] 增加采集样本数（目标 12-15）
- [ ] 调整置信度阈值（当前 60%，建议 __）
- [ ] 调整通道权重（当前 ch1:0.30, ch2:0.60, ch3:0.10）
- [ ] 清理历史数据
- [ ] 重新选择指令（当前指令容易混淆）

---

## 📤 交付清单

完成诊断后，请提供以下文件：

1. **diagnosis-YYYY-MM-DD.json** - 诊断数据
2. **recognition-results-YYYY-MM-DD.json** - 识别结果
3. **emg-data-*.json** - EMG 原始数据（可选）
4. **诊断报告.md** - 诊断报告（按上述模板）

---

## ⏱️ 预计时间

- 采集训练样本：20-30 分钟
- 进行识别测试：20-30 分钟
- 导出和分析数据：10-15 分钟
- **总计：50-75 分钟**

---

## ❓ 常见问题

### Q1：采集时应该怎样默念指令？
**A**：清晰、自然、一致。不要过度夸张或过度轻声。保持 2-3 秒的默念时间。

### Q2：如果某个指令的准确率特别低怎么办？
**A**：这是诊断阶段的正常现象。记录下来，在诊断报告中分析原因（可能是指令选择不当、采集质量差或通道权重不合理）。

### Q3：导出的 JSON 文件很大怎么办？
**A**：正常现象。每个识别试验包含完整的特征数据。可以用文本编辑器或 JSON 查看器打开。

### Q4：如果识别结果全是错的怎么办？
**A**：这表明系统可能有严重问题。请检查：
- 硬件连接是否正常
- 采集时是否真的默念了指令
- 是否选择了错误的指令

---

## 🎯 下一步

完成诊断后，根据诊断结论决定是否进入 Phase 3（算法优化）。

**如果准确率 ≥ 70%**：系统工作正常，可以考虑微调阈值或权重。

**如果准确率 < 70%**：需要分析具体原因后再决定改进方案。

---

**祝诊断顺利！** 📊
