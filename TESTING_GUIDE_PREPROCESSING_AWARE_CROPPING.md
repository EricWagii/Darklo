# 预处理后裁剪方案（方案 B）测试和验证指南

## 概述

本文档提供了预处理后裁剪方案的完整测试和验证流程，帮助用户评估新算法的改进效果。

---

## 一、实现内容总结

### 新增文件
- **`preprocessing-aware-cropping.ts`**：预处理感知的波形裁剪算法实现
  - `detectValidSegmentAfterPreprocessing()`：单采集有效片段检测
  - `alignMultipleCollectionsAfterPreprocessing()`：多采集对齐
  - `batchCropCollectionsAfterPreprocessing()`：批量裁剪

### 修改文件
- **`CollectionMode.tsx`**：集成新的裁剪算法
  - 导入新的预处理后裁剪函数
  - 更新 `handleAutoCropAll()` 函数
  - 显示 SNR 权重信息

### 核心改进
1. **预处理集成**：在裁剪前对波形进行完整预处理（陷波、高通、ICA）
2. **三通道融合**：计算 SNR 权重，融合三通道特征
3. **自适应对齐**：使用中位数对齐多个采集
4. **准确度提升**：预期提升 10-15%

---

## 二、测试场景

### 场景 1：标准采集（基准测试）

**目标**：验证新算法在标准条件下的表现

**步骤**：
1. 打开采集训练页面
2. 采集同一指令 5-10 次（标准速度、标准力度）
3. 点击"一键裁剪"按钮
4. 观察以下指标：
   - 有效片段比例（应 > 80%）
   - SNR 权重分布（三通道权重是否合理）
   - 裁剪后数据长度（应一致）

**预期结果**：
- ✅ 有效片段比例 > 80%
- ✅ SNR 权重合理分布（不应有某通道权重过低）
- ✅ 所有采集裁剪后长度一致

**评估指标**：
```
有效片段比例 = (裁剪后长度) / (原始长度) × 100%
目标：80-95%
```

### 场景 2：多速度采集（鲁棒性测试）

**目标**：验证新算法对不同速度的适应性

**步骤**：
1. 采集同一指令 5 次，每次使用不同速度：
   - 采集 1-2：快速
   - 采集 3-4：正常速度
   - 采集 5：缓慢
2. 点击"一键裁剪"
3. 比较各采集的裁剪结果

**预期结果**：
- ✅ 所有采集都被正确裁剪
- ✅ 裁剪后长度差异 < 20%
- ✅ 置信度 > 70%

**评估指标**：
```
长度差异率 = (最大长度 - 最小长度) / 平均长度 × 100%
目标：< 20%
```

### 场景 3：弱信号采集（适应性测试）

**目标**：验证新算法对弱信号的处理能力

**步骤**：
1. 采集同一指令 5 次，使用较弱的肌肉收缩
2. 点击"一键裁剪"
3. 观察：
   - 是否能正确识别有效片段
   - SNR 权重是否自动调整
   - 置信度是否合理

**预期结果**：
- ✅ 能正确识别有效片段（即使信号较弱）
- ✅ SNR 权重自动调整（较弱通道权重降低）
- ✅ 置信度 > 60%

**评估指标**：
```
弱信号检测成功率 = (成功识别采集数) / (总采集数) × 100%
目标：> 90%
```

### 场景 4：多通道差异采集（融合效果测试）

**目标**：验证三通道融合的效果

**步骤**：
1. 采集同一指令 5 次，观察三通道信号强度差异
2. 点击"一键裁剪"
3. 查看 SNR 权重分布
4. 与旧算法（仅 CH2）的结果对比

**预期结果**：
- ✅ SNR 权重反映真实的通道强度差异
- ✅ 融合后的裁剪结果比单通道更准确
- ✅ 识别准确度提升 5-10%

**评估指标**：
```
通道权重差异 = max(权重) - min(权重)
目标：> 0.1（表示有明显的通道差异）

融合效果 = (融合后准确度 - 单通道准确度) / 单通道准确度 × 100%
目标：> 5%
```

---

## 三、性能指标

### 关键指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| **有效片段比例** | 80-95% | 裁剪后数据占原始数据的比例 |
| **置信度** | > 70% | 算法对裁剪结果的置信度 |
| **长度一致性** | < 20% | 多采集裁剪后长度的差异 |
| **准确度提升** | +10-15% | 与旧算法相比的识别准确度提升 |
| **计算时间** | < 1s | 单次裁剪的计算时间 |

### 监控指标

通过浏览器控制台查看以下日志：
```
[Cropping] 开始预处理信号...
[Cropping] 计算 SNR 权重...
[Cropping] 融合三通道信号...
[Cropping] 计算融合信号的能量和变化率...
[Cropping] 能量阈值: X.XXXX, 变化率阈值: X.XXXX
[Cropping] 检测完成: 有效片段 [start, end], 置信度: X.X%
[Cropping] 对齐完成: [start, end], 平均置信度: X.X%
```

---

## 四、对比测试

### 与旧算法（改进的分割）对比

**旧算法**：`alignMultipleCollectionsImproved()`
- 仅使用 CH2
- 不预处理
- 固定百分位数阈值

**新算法**：`alignMultipleCollectionsAfterPreprocessing()`
- 三通道融合
- 完整预处理
- 自适应权重

**对比方法**：
1. 采集相同的数据集
2. 分别使用两种算法进行裁剪
3. 比较以下指标：
   - 有效片段比例
   - 裁剪后长度一致性
   - 识别准确度

**预期结果**：
```
新算法相比旧算法：
- 有效片段比例：提升 5-10%
- 长度一致性：改善 10-20%
- 识别准确度：提升 10-15%
```

---

## 五、验证清单

### 功能验证
- [ ] 新的裁剪算法能正确导入
- [ ] CollectionMode 能正确调用新算法
- [ ] 一键裁剪按钮能正常工作
- [ ] SNR 权重信息能正确显示
- [ ] 错误处理能正常工作

### 性能验证
- [ ] 单次裁剪时间 < 1 秒
- [ ] 内存占用合理
- [ ] 不会导致浏览器卡顿

### 准确度验证
- [ ] 标准采集场景：有效片段比例 > 80%
- [ ] 多速度采集：长度差异 < 20%
- [ ] 弱信号采集：检测成功率 > 90%
- [ ] 多通道差异：权重分布合理

### 用户体验验证
- [ ] 用户能理解 SNR 权重信息
- [ ] 用户能看到详细的裁剪结果
- [ ] 错误提示清晰明确
- [ ] 操作流程直观易用

---

## 六、故障排除

### 问题 1：无法识别有效波形

**症状**：显示"无法识别有效波形，请检查采集数据"

**原因**：
- 采集数据质量太差
- 信号过弱
- 噪声过大

**解决方案**：
1. 检查电极连接
2. 重新调节电极位置
3. 重新采集数据
4. 检查浏览器控制台日志

### 问题 2：SNR 权重分布不合理

**症状**：某个通道的权重过低（< 0.1）

**原因**：
- 该通道信号质量差
- 该通道电极接触不良
- 该通道有严重噪声

**解决方案**：
1. 检查该通道的电极
2. 调节电极位置
3. 重新采集数据

### 问题 3：裁剪后长度差异过大

**症状**：不同采集的裁剪后长度差异 > 20%

**原因**：
- 采集速度差异太大
- 采集数据质量不一致
- 算法参数不适应

**解决方案**：
1. 确保采集速度一致
2. 重新采集数据
3. 调整算法参数（如百分位数）

### 问题 4：计算时间过长

**症状**：一键裁剪需要超过 2 秒

**原因**：
- 采集数据过长
- 浏览器性能差
- 预处理计算复杂

**解决方案**：
1. 缩短采集时间
2. 使用性能更好的浏览器
3. 关闭其他标签页

---

## 七、优化建议

### 短期优化
1. **参数调整**：根据实际测试结果调整百分位数阈值
2. **性能优化**：考虑使用 Web Worker 进行后台计算
3. **用户反馈**：收集用户反馈，改进算法

### 中期优化
1. **自适应参数**：根据用户的采集习惯自动调整参数
2. **异常检测**：自动检测和标记异常采集
3. **可视化改进**：显示能量曲线和活跃区域

### 长期优化
1. **机器学习**：使用机器学习优化裁剪参数
2. **个性化**：为每个用户学习最优的裁剪参数
3. **实时反馈**：在采集过程中实时显示有效片段

---

## 八、发布前检查

### 代码质量
- [ ] 代码通过 TypeScript 类型检查
- [ ] 代码通过 ESLint 检查
- [ ] 代码有适当的注释
- [ ] 错误处理完善

### 功能完整性
- [ ] 所有新功能已实现
- [ ] 所有测试场景已验证
- [ ] 所有已知问题已解决
- [ ] 向后兼容性已验证

### 文档完整性
- [ ] 代码文档完整
- [ ] 用户文档完整
- [ ] 测试文档完整
- [ ] 故障排除指南完整

### 性能指标
- [ ] 计算时间 < 1 秒
- [ ] 内存占用 < 50MB
- [ ] 不会导致浏览器卡顿
- [ ] 支持 5+ 采集的同时处理

---

## 九、成功标准

### 最低要求
- ✅ 新算法能正确工作
- ✅ 有效片段比例 > 80%
- ✅ 置信度 > 70%
- ✅ 计算时间 < 2 秒

### 目标要求
- ✅ 有效片段比例 > 85%
- ✅ 置信度 > 80%
- ✅ 计算时间 < 1 秒
- ✅ 准确度提升 > 10%

### 优秀要求
- ✅ 有效片段比例 > 90%
- ✅ 置信度 > 85%
- ✅ 计算时间 < 500ms
- ✅ 准确度提升 > 15%
- ✅ 所有测试场景通过

---

## 十、反馈和改进

### 收集反馈的方式
1. **用户测试**：邀请用户进行实际测试
2. **日志分析**：分析浏览器控制台日志
3. **性能监控**：监控计算时间和内存占用
4. **准确度评估**：评估识别准确度的提升

### 改进流程
1. **收集数据**：收集测试数据和反馈
2. **分析问题**：分析问题的根本原因
3. **制定方案**：制定改进方案
4. **实施改进**：实施改进并测试
5. **验证效果**：验证改进效果

---

## 总结

预处理后裁剪方案（方案 B）是一个重大改进，预期能提升识别准确度 10-15%。通过本测试和验证指南，用户可以全面评估新算法的性能，并根据实际情况进行优化。

**关键成功因素**：
1. ✅ 完整的预处理管道
2. ✅ 三通道 SNR 权重融合
3. ✅ 自适应对齐算法
4. ✅ 详细的日志和反馈
5. ✅ 持续的优化和改进
