# 改进的动态裁剪算法原理详解

## 📌 核心问题

原有的多峰值裁剪算法存在以下问题：

1. **阈值计算不够智能**
   - 使用固定系数：`threshold = minEnergy + (maxEnergy - minEnergy) * 0.15`
   - 对所有波形使用相同的0.15系数，无法适应不同特征的波形
   - 能量明显的波形可能过度裁剪，能量不明显的波形可能不足

2. **无法区分前后空白和中间低能量区域**
   - 简单地找到超过阈值的起始和结束位置
   - 可能误删中间的低能量区域（这些是有效信号的一部分）

3. **没有诊断信息**
   - 裁剪失败时用户完全不知道
   - 无法了解失败的原因

## 🎯 新算法的核心思想

**基于能量差异的动态裁剪**

新算法的核心是：**根据波形的能量分布特征，动态计算合适的阈值**

```
原始波形能量分布
│
├─ 前空白区域（低能量）
│
├─ 有效信号区域（高能量）
│  ├─ 峰值1（高能量）
│  ├─ 中间低能量区域（低但有效）← 保留！
│  └─ 峰值2（高能量）
│
└─ 后空白区域（低能量）
```

## 📊 算法的5个关键步骤

### 步骤1：计算能量统计信息

```
对每个窗口计算RMS能量：
energy[i] = sqrt(sum(signal[i:i+windowSize]²) / windowSize)

然后计算统计量：
- min: 最小能量
- max: 最大能量
- mean: 平均能量
- stdDev: 标准差
- median: 中位数
```

**目的**：了解波形的能量分布特征

**示例**：
```
波形A（能量明显）：min=0.01, max=0.8, mean=0.3, stdDev=0.2
波形B（能量不明显）：min=0.05, max=0.15, mean=0.1, stdDev=0.02
```

### 步骤2：动态计算阈值

这是新算法最关键的改进！

**关键指标：变异系数（Coefficient of Variation）**
```
CV = stdDev / mean
```

**阈值计算逻辑**：

```python
if energyRange < 0.01:
    # 信号很弱，使用低敏感度阈值
    threshold = min + energyRange * 0.3
    reason = "信号很弱"

elif CV > 0.3:
    # 能量分布明显（峰值突出）
    # 使用基于stdDev的阈值，可以更好地检测峰值
    threshold = mean - stdDev * 0.5
    reason = "能量分布明显，使用stdDev阈值"

else:
    # 能量分布不明显（信号平缓）
    # 使用基于能量范围的阈值，更灵敏
    threshold = min + energyRange * 0.2
    reason = "能量分布不明显，使用范围阈值"
```

**为什么这样设计？**

- **能量明显的波形**（如"yeah"）：峰值突出，stdDev大
  - 使用 `mean - stdDev * 0.5` 可以准确定位峰值
  - 避免过度裁剪

- **能量不明显的波形**（如"嗯"）：信号平缓，stdDev小
  - 使用 `min + energyRange * 0.2` 更灵敏
  - 避免遗漏有效信号

### 步骤3：识别前后空白

```
从头开始扫描：
for i = 0 to length:
    if energy[i] > threshold:
        frontEnd = i  ← 第一个超过阈值的点
        break

从尾开始扫描：
for i = length-1 to 0:
    if energy[i] > threshold:
        rearStart = i + 1  ← 最后一个超过阈值的点
        break
```

**关键点**：
- 只从头和尾扫描，不会删除中间的低能量区域
- 即使中间有低能量区域，也会被保留

**示例**：
```
能量序列：[0.01, 0.02, 0.5, 0.1, 0.6, 0.02, 0.01]
阈值：0.1

前空白：[0.01, 0.02] → frontEnd = 2
有效段：[0.5, 0.1, 0.6]  ← 中间的0.1被保留！
后空白：[0.02, 0.01] → rearStart = 6
```

### 步骤4：识别中间低能量区域（可选）

这一步是为了诊断和记录，不影响裁剪结果。

```
在有效段内，找到能量明显低于平均值的区域：
for i = frontEnd to rearStart:
    if energy[i] < mean * 0.5 and energy[i] > threshold * 0.5:
        记录这个区域
```

**目的**：
- 帮助用户理解哪些区域被保留
- 用于诊断和调试

### 步骤5：质量检查

```
validSegmentLength = rearStart - frontEnd
confidence = validSegmentLength / totalLength

if validSegmentLength < 100 or confidence < 0.2:
    isQualityAcceptable = false
    qualityReason = "有效段过短" 或 "置信度过低"
else:
    isQualityAcceptable = true
```

## 🔄 与原算法的对比

| 方面 | 原算法 | 新算法 |
|------|--------|--------|
| **阈值计算** | 固定系数 0.15 | 动态系数（0.2-0.5） |
| **适应性** | 所有波形相同 | 根据能量分布自适应 |
| **中间低能量** | 可能被误删 | 保留 |
| **诊断信息** | 无 | 详细的能量统计和区域信息 |
| **失败提示** | 无 | 用户对话框提示 |

## 📈 为什么这样设计能提高准确度？

### 原因1：正确的裁剪范围
- 新算法能够准确识别有效信号的边界
- 保留了中间的低能量区域（这些是有效信号的一部分）
- 避免了过度裁剪或不足裁剪

### 原因2：特征一致性
- 采集和测试使用相同的裁剪算法
- 采集时的特征提取和测试时的特征提取基于相同的裁剪范围
- 避免了特征不匹配导致的识别失败

### 原因3：用户反馈
- 裁剪失败时用户能够看到诊断信息
- 用户可以了解失败的原因并重新采集
- 避免使用质量不达标的数据进行识别

## 🎓 实际例子

### 例子1：能量明显的波形（"yeah"）

```
原始能量序列：
[0.01, 0.02, 0.03, 0.5, 0.6, 0.7, 0.5, 0.3, 0.02, 0.01]

能量统计：
- min = 0.01
- max = 0.7
- mean = 0.29
- stdDev = 0.26
- CV = 0.26 / 0.29 = 0.90 > 0.3 ✓

阈值计算：
threshold = mean - stdDev * 0.5
         = 0.29 - 0.26 * 0.5
         = 0.29 - 0.13
         = 0.16

裁剪结果：
前空白：[0.01, 0.02, 0.03]
有效段：[0.5, 0.6, 0.7, 0.5, 0.3]  ← 完整保留
后空白：[0.02, 0.01]
```

### 例子2：能量不明显的波形（"嗯"）

```
原始能量序列：
[0.05, 0.06, 0.1, 0.12, 0.11, 0.08, 0.06, 0.05]

能量统计：
- min = 0.05
- max = 0.12
- mean = 0.085
- stdDev = 0.025
- CV = 0.025 / 0.085 = 0.29 < 0.3 ✓

阈值计算：
threshold = min + energyRange * 0.2
         = 0.05 + (0.12 - 0.05) * 0.2
         = 0.05 + 0.014
         = 0.064

裁剪结果：
前空白：[0.05]
有效段：[0.06, 0.1, 0.12, 0.11, 0.08, 0.06]  ← 完整保留
后空白：[0.05]
```

### 例子3：中间有低能量区域的波形（"yeah-yeah"）

```
原始能量序列：
[0.01, 0.02, 0.5, 0.6, 0.1, 0.08, 0.5, 0.6, 0.02, 0.01]
                  ↑峰值1    ↑低能量   ↑峰值2

能量统计：
- min = 0.01
- max = 0.6
- mean = 0.28
- stdDev = 0.24
- CV = 0.86 > 0.3 ✓

阈值计算：
threshold = mean - stdDev * 0.5 = 0.16

裁剪结果：
前空白：[0.01, 0.02]
有效段：[0.5, 0.6, 0.1, 0.08, 0.5, 0.6]  ← 中间的0.1, 0.08被保留！
后空白：[0.02, 0.01]

这样做的好处：
- 保留了两个峰值之间的连接
- 特征提取能够捕捉到完整的"yeah-yeah"模式
- 识别准确度更高
```

## 🔍 诊断信息的作用

新算法返回详细的诊断信息：

```typescript
diagnostics: {
  energyStats: {
    min: 0.01,
    max: 0.7,
    mean: 0.29,
    stdDev: 0.26,
    median: 0.25
  },
  threshold: 0.16,
  thresholdReason: "能量分布明显，使用stdDev阈值",
  frontBlankLength: 30,      // 前空白30个样本
  rearBlankLength: 20,       // 后空白20个样本
  validSegmentLength: 950,   // 有效段950个样本
  midLowEnergyRegions: [     // 中间低能量区域
    {
      startIdx: 300,
      endIdx: 350,
      reason: "中间低能量区域，保留为有效信号的一部分"
    }
  ]
}
```

这些信息可以：
- 帮助用户理解裁剪过程
- 用于调试和优化
- 在裁剪失败时提供诊断依据

## 📝 总结

新的动态裁剪算法通过：

1. **智能的阈值计算** - 根据能量分布自适应
2. **正确的边界识别** - 从头尾扫描，保留中间低能量区域
3. **详细的诊断信息** - 帮助用户理解和调试
4. **用户友好的反馈** - 失败时提示原因

这些改进能够显著提高采集和测试的准确度。
