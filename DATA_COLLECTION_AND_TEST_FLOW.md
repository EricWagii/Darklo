# Ear EMG 数据采集和测试逻辑流程

## 1. 数据采集流程（Collection Mode）

### 1.1 采集前准备
```
用户登录 → 进入采集训练页面 → 输入指令名称 → 检查指令是否已存在
```

### 1.2 采集过程
```
点击"开始采集" 
  ↓
启动硬件采集（3通道肌电信号）
  ↓
实时显示波形（ch1, ch2, ch3）
  ↓
采集时间达到设定时长（通常5-10秒）
  ↓
采集完成，显示完整波形
```

### 1.3 波形裁剪（关键步骤）
```
采集完成 → 自动进行两步裁剪
  ├─ 第一步：自适应阈值裁剪
  │   ├─ 计算RMS和方差
  │   ├─ 生成自适应阈值
  │   ├─ 从头找到第一个超过阈值的点（前空白）
  │   └─ 从尾找到最后一个超过阈值的点（后空白）
  │
  └─ 第二步：有效段检测
      ├─ 检测有效信号段
      ├─ 保留中间低能量区域（不裁剪）
      └─ 返回裁剪后的波形
```

### 1.4 质量评估
```
裁剪后的波形 → 计算质量评分
  ├─ 能量评分（Energy Score）
  ├─ 一致性评分（Consistency Score）
  ├─ 信噪比评分（SNR Score）
  └─ 综合评分（Overall Score）
```

### 1.5 异常检测
```
质量评分 → 与历史采集比对
  ├─ 如果是首次采集：需要≥5条才进行异常检测
  ├─ 如果是追加采集：与历史数据整体比对
  └─ 检测到异常 → 弹出对话框提示用户删除
```

### 1.6 数据保存
```
用户确认保存
  ↓
转换数据格式
  ↓
保存到IndexedDB
  ↓
更新UI显示
  ↓
清空采集页面，等待下一个指令
```

---

## 2. 默念测试流程（Recognition Mode）

### 2.1 测试前准备
```
进入默念测试页面 → 加载已保存的指令库
  ├─ 从IndexedDB读取所有指令
  ├─ 显示可用指令列表
  └─ 选择要测试的指令
```

### 2.2 测试过程
```
选择指令 → 点击"开始测试"
  ↓
启动硬件采集（新的肌电信号）
  ↓
实时显示波形
  ↓
采集时间达到设定时长
  ↓
测试采集完成
```

### 2.3 测试数据裁剪（关键步骤）
```
测试采集完成 → 自动进行两步裁剪
  ├─ 第一步：自适应阈值裁剪（同采集过程）
  │   ├─ 计算RMS和方差
  │   ├─ 生成自适应阈值
  │   ├─ 从头找到第一个超过阈值的点（前空白）
  │   └─ 从尾找到最后一个超过阈值的点（后空白）
  │
  └─ 第二步：有效段检测（同采集过程）
      ├─ 检测有效信号段
      ├─ 保留中间低能量区域（不裁剪）
      └─ 返回裁剪后的波形
```

### 2.4 特征提取
```
裁剪后的波形 → 提取180维特征
  ├─ 时域特征（60维）
  ├─ 频域特征（30维）
  ├─ MFCC特征（39维）
  └─ 梅尔谱图特征（21维）
```

### 2.5 识别匹配
```
测试特征 → 与指令库中所有采集进行匹配
  ├─ CNN模型识别
  ├─ DTW动态时间规整
  ├─ 多通道融合（SNR权重）
  └─ 返回最相似的指令和置信度
```

### 2.6 结果显示
```
识别完成 → 显示结果
  ├─ 匹配的指令名称
  ├─ 置信度评分
  ├─ 相似度排名
  └─ 建议用户重试或调整
```

---

## 3. 当前存在的问题

### 问题1：裁剪失败但无提示
**症状：** 测试结果准确度极低，日志显示裁剪未被成功进行，但用户界面没有任何提示

**原因分析：**
- 裁剪失败时没有向用户显示错误提示
- 系统继续使用未裁剪或部分裁剪的波形进行识别
- 导致特征提取和匹配结果不准确

**影响：** 用户无法了解数据质量问题，误认为系统识别能力差

### 问题2：裁剪算法不够智能
**当前算法问题：**
- 使用固定的自适应阈值
- 不考虑波形能量的动态变化
- 对于能量明显和不明显的波形，使用相同的标准
- 可能误删中间的低能量区域（有效信号的一部分）

**需要改进：**
- 基于能量差异的动态标准
- 区分前后空白和中间低能量区域
- 为不同特征的波形生成不同的裁剪标准

### 问题3：采集和测试的裁剪算法不一致
**当前状态：** 两个模块可能使用不同的裁剪逻辑或参数

**影响：** 采集时的波形和测试时的波形经过不同的处理，导致特征不匹配

---

## 4. 改进方向

### 4.1 改进裁剪算法
```
新的动态裁剪算法：

1. 计算能量分布
   ├─ 计算每个时间点的能量
   ├─ 计算能量的均值和标准差
   └─ 生成能量曲线

2. 识别有效信号区间
   ├─ 找到能量显著高于平均值的区间
   ├─ 这些区间被认为是"有效信号"
   └─ 记录有效信号的起始和结束位置

3. 识别前后空白
   ├─ 前空白：从头到第一个有效信号的距离
   ├─ 后空白：从最后一个有效信号到尾的距离
   └─ 这些区域的能量与有效信号差异很大

4. 识别中间低能量区域
   ├─ 在有效信号区间内的低能量区域
   ├─ 这些区域可能是肌肉放松或信号暂停
   ├─ 但仍属于有效信号的一部分
   └─ 不应该被裁剪

5. 执行裁剪
   ├─ 删除前空白
   ├─ 删除后空白
   ├─ 保留中间的所有内容（包括低能量区域）
   └─ 返回裁剪后的波形
```

### 4.2 添加诊断日志
```
在裁剪过程中添加详细日志：
- 能量分析结果
- 有效信号区间识别
- 前后空白识别
- 中间低能量区域识别
- 裁剪决策和结果
- 用户界面提示（如果裁剪失败）
```

### 4.3 确保一致性
```
- 采集模块和测试模块使用相同的裁剪算法
- 使用相同的参数和阈值
- 在两个模块中都添加相同的诊断日志
```

---

## 5. 数据流向图

```
┌─────────────────────────────────────────────────────────────┐
│                    Ear EMG 系统数据流                        │
└─────────────────────────────────────────────────────────────┘

采集训练模块                          默念测试模块
─────────────────                     ──────────────

硬件采集 (5-10s)                      硬件采集 (5-10s)
    ↓                                     ↓
原始波形 (ch1,ch2,ch3)                原始波形 (ch1,ch2,ch3)
    ↓                                     ↓
┌──────────────────────────────────────────────────┐
│  两步裁剪算法（采集和测试保持一致）              │
│  ├─ 第一步：自适应阈值裁剪                      │
│  └─ 第二步：有效段检测                          │
└──────────────────────────────────────────────────┘
    ↓                                     ↓
裁剪后波形                            裁剪后波形
    ↓                                     ↓
质量评估                              特征提取
(Energy, Consistency, SNR)            (180维特征)
    ↓                                     ↓
异常检测                              识别匹配
(与历史数据比对)                      (CNN + DTW)
    ↓                                     ↓
用户确认                              显示结果
    ↓                                     ↓
保存到IndexedDB                       用户查看
    ↓
指令库更新
```

---

## 6. 关键参数和阈值

| 参数 | 当前值 | 说明 |
|------|-------|------|
| 采集时长 | 5-10s | 单次采集的时间长度 |
| 采样率 | 1000Hz | 每秒采样点数 |
| 自适应阈值系数 | 0.65 | 用于计算阈值的系数 |
| 异常检测阈值 | 50 | 质量评分低于此值认为异常 |
| 首次采集最少条数 | 5 | 首次采集需要至少5条才进行异常检测 |

---

## 7. 待改进事项清单

- [ ] 重新设计动态裁剪算法
- [ ] 实现基于能量差异的阈值
- [ ] 添加详细的诊断日志
- [ ] 在UI中显示裁剪失败提示
- [ ] 确保采集和测试的裁剪算法一致
- [ ] 验证新算法的准确度提升
- [ ] 添加裁剪过程的可视化调试工具
