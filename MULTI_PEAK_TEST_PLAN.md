# 多峰值信号处理测试计划

## 测试目标

验证多峰值能量检测算法是否能正确处理复杂指令（如"yeah"），提升识别准确率。

## 测试场景

### 场景 1：单峰值指令（基准测试）
- **指令**：666、888、10、444
- **预期**：
  - 使用单峰值策略（`detectionStrategy: 'single'`）
  - 峰值数量 = 1
  - 识别准确率 ≥ 90%（相比之前的 80%）

### 场景 2：多峰值指令（新增支持）
- **指令**：yeah
- **预期**：
  - 使用多峰值策略（`detectionStrategy: 'multi'`）
  - 峰值数量 ≥ 2
  - 识别准确率 ≥ 70%（从之前的 ~10% 大幅提升）

### 场景 3：混合指令集
- **指令**：666, 888, 10, 444, yeah
- **预期**：
  - 系统能自动区分单峰值和多峰值指令
  - 整体识别准确率 ≥ 80%

## 测试流程

### 步骤 1：采集训练
1. 连接 STM32 设备
2. 对每个指令采集 5-10 次
3. 观察日志输出：
   ```
   多峰值裁切完成: startIdx=..., endIdx=..., 峰值数=..., 策略=...
   📊 多峰值采集统计: X/Y 采集使用多峰值策略
   平均峰值数: Z
   ```

### 步骤 2：验证采集数据
1. 进入"数据管理"页面
2. 查看每个指令的波形缩小图
3. 验证：
   - 单峰值指令显示单一集中的峰值
   - 多峰值指令显示多个分散的峰值
   - 所有采集都被正确保存

### 步骤 3：测试识别
1. 进入"默念测试"页面
2. 依次识别每个指令（至少 10 次）
3. 记录识别结果：
   - 识别成功/失败
   - 置信度
   - 是否使用了多峰值策略

### 步骤 4：性能分析
1. 查看日志中的性能指标
2. 验证：
   - 采集时的裁切耗时 < 500ms
   - 识别时的裁切耗时 < 100ms
   - 没有明显的 UI 卡顿

## 预期日志输出示例

### 采集阶段
```
多峰值裁切完成: startIdx=150, endIdx=850, 峰值数=2, 策略=multi
📊 多峰值采集统计: 3/5 采集使用多峰值策略
   平均峰值数: 1.8
```

### 识别阶段
```
[多峰值检测] min=0.050, max=0.320, median=0.150, stdDev=0.080, threshold=0.190
[峰值检测] 识别到 2 个峰值
  峰值 1: [200, 400], 能量=0.280, 持续=200
  峰值 2: [550, 750], 能量=0.250, 持续=200
[峰值合并] 峰值数量: 2 → 2 (无合并)
[区间选择] 多峰值策略：并集 [200, 750], 包含 2 个峰值
```

## 成功标准

| 指标 | 目标值 | 验证方法 |
|------|--------|---------|
| 单峰值指令识别准确率 | ≥ 90% | 测试 666, 888, 10, 444 各 10 次 |
| 多峰值指令识别准确率 | ≥ 70% | 测试 yeah 10 次 |
| 整体识别准确率 | ≥ 80% | 混合指令集测试 |
| 采集裁切耗时 | < 500ms | 查看日志中的耗时 |
| 识别裁切耗时 | < 100ms | 查看日志中的耗时 |
| 多峰值采集比例 | > 50% | 对 yeah 指令的采集统计 |

## 故障排查

### 问题 1：多峰值指令仍然识别率低
**可能原因**：
- 峰值合并参数不合适（minGap 过大或过小）
- 能量阈值计算不准确

**解决方案**：
1. 检查日志中的峰值识别结果
2. 调整 `minGap` 参数（当前：50 样本）
3. 调整能量阈值计算方式

### 问题 2：单峰值指令被错误识别为多峰值
**可能原因**：
- 能量检测阈值过低，噪声被识别为峰值

**解决方案**：
1. 提高能量阈值
2. 增加最小峰值持续时间限制

### 问题 3：采集保存失败
**可能原因**：
- 多峰值检测返回无效的裁切参数

**解决方案**：
1. 检查 `startIdx >= endIdx` 的情况
2. 查看降级处理是否被触发
3. 验证波形数据的有效性

## 优化建议

### 短期优化
1. 根据实际测试结果调整 `minGap` 参数
2. 优化能量阈值计算（当前使用中位数 + 标准差）
3. 添加更详细的诊断日志

### 中期优化
1. 实现自适应的 `minGap` 参数（基于信号特性）
2. 支持用户自定义的峰值合并策略
3. 添加波形分析工具，帮助用户理解为什么某些指令识别率低

### 长期优化
1. 使用机器学习方法自动学习最优的峰值合并参数
2. 支持更复杂的多峰值模式识别
3. 集成 DTW 算法处理速度差异导致的识别偏差

## 测试记录

### 测试日期：[待填]
- 测试人员：[待填]
- 设备型号：[待填]
- 测试结果：[待填]

### 指令识别准确率
| 指令 | 采集次数 | 识别次数 | 成功次数 | 准确率 | 平均峰值数 |
|------|---------|---------|---------|--------|-----------|
| 666 | - | - | - | - | - |
| 888 | - | - | - | - | - |
| 10 | - | - | - | - | - |
| 444 | - | - | - | - | - |
| yeah | - | - | - | - | - |

### 性能指标
| 指标 | 值 |
|------|-----|
| 采集裁切耗时（平均） | - |
| 识别裁切耗时（平均） | - |
| 多峰值采集比例 | - |
| 系统稳定性 | - |

## 参考资源

- 设计文档：`MULTI_PEAK_DESIGN.md`
- 实现代码：`client/src/lib/multi-peak-detection.ts`
- 采集流程：`client/src/pages/CollectionMode.tsx`
- 识别流程：`client/src/pages/RecognitionMode.tsx`
