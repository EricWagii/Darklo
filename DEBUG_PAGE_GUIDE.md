# DEBUG调试页面使用指南

## 概述

DEBUG调试页面是一个专门为诊断信号处理流程而设计的工具。它提供了波形可视化、能量分析、裁切参数检查和时长统计等功能，帮助用户精确分析采集和识别过程中可能出现的问题。

## 访问方式

1. **登录系统** - 输入用户名和密码
2. **点击首页的"🔧 DEBUG"按钮** - 进入DEBUG调试页面
3. **选择要分析的指令和采集序号** - 点击"加载数据"

## 页面功能详解

### 1. 数据加载面板 (📂 数据加载)

**功能**：选择和加载采集数据

**操作步骤**：
- 从下拉菜单中选择一条指令
- 如果该指令有多条采集，使用滑块选择要查看的采集序号
- 点击"加载数据"按钮

**显示信息**：
- 选中指令的采集总数
- 当前选中的采集序号

### 2. 波形可视化 (📊 原始波形)

**功能**：显示原始EMG信号的波形图

**图表说明**：
- **蓝色曲线** - 原始波形数据
- **绿线** - 裁切起点 (startIdx)
- **红线** - 裁切终点 (endIdx)
- **蓝色半透明区域** - 有效裁切范围
- **网格** - 便于读取数值的参考网格

**交互功能**：
- 将鼠标悬停在图表上，可以看到该位置的具体样本值
- 实时显示样本序号和对应的数值

**关键指标**：
- **长度** - 原始波形的样本数
- **时长** - 原始波形的毫秒数（关键：用于判断发音时长）

### 3. 能量分布 (⚡ 能量分布)

**功能**：显示信号能量随时间的变化

**图表说明**：
- **橙色填充区域** - 能量分布（面积图）
- **红色虚线** - 能量阈值（高于此线为有效信号）
- **绿色虚线** - 有效段范围

**交互功能**：
- 将鼠标悬停在图表上，查看该位置的能量值

**关键指标**：
- **阈值** - 用于判断有效信号的能量阈值
- **置信度** - 有效段内高于阈值的比例（0-100%）
- **峰值数** - 检测到的峰值个数

### 4. 裁切参数 (🎯 裁切参数)

**功能**：显示信号处理的关键参数

**参数说明**：

| 参数 | 含义 | 说明 |
|------|------|------|
| 原始长度 | 样本数 | 原始波形的总样本数 |
| 裁切起点 | 样本索引 | 有效信号开始的位置 |
| 裁切终点 | 样本索引 | 有效信号结束的位置 |
| 裁切长度 | 样本数 | 有效信号的样本数 |
| 裁切时长 | 毫秒 | 有效信号的时长 |
| 峰值数 | 个数 | 检测到的能量峰值个数 |
| 检测策略 | 类型 | single-peak（单峰值）或 multi-peak（多峰值） |

**质量评分**：
- **评分** - 0-100 的综合质量评分
- **星级** - 用星号表示的质量等级（★★★★★）
- **进度条** - 可视化的评分显示

**质量评价标准**：
- **80-100分** - ✅ 优秀（绿色）
- **60-79分** - ⚠️ 良好（黄色）
- **40-59分** - ⚠️ 一般（橙色）
- **0-39分** - ❌ 不佳（红色）

### 5. 时长统计 (⏱️ 时长统计)

**功能**：显示信号在各个处理阶段的时长变化

**关键指标**：

| 阶段 | 说明 | 重要性 |
|------|------|--------|
| 原始波形 | 从采集开始到停止的总时长 | ⭐⭐⭐ 最重要 |
| 裁切后 | 去除静音后的有效信号时长 | ⭐⭐ 重要 |
| 归一化 | 固定为 1.024 秒（512样本） | ⭐ 参考 |

**时长变化百分比**：
- 显示每个阶段相比前一阶段的时长变化
- 绿色表示减少，红色表示增加

**关键发现**：
- 如果原始波形超过 1 秒，表示发音较长（多音节或带停顿）
- 如果裁切后仍超过 1.024 秒，可能导致信号被截断
- 建议检查是否需要调整 FIXED_WAVEFORM_LENGTH 参数

### 6. 对比分析 (🔄 对比分析)

**功能**：评估采集数据的质量和对齐情况

**关键指标**：

| 指标 | 含义 | 评分标准 |
|------|------|---------|
| 相关性 | 波形相似度 | >70% 优秀，50-70% 良好，<50% 需改进 |
| 对齐得分 | 综合对齐评分 | ≥80 优秀，60-79 良好，<60 需改进 |
| 峰值数 | 检测到的峰值个数 | 0 个表示无有效信号 |
| 质量评分 | 整体质量评分 | 同质量评分标准 |

**建议**：
- 页面会根据各项指标给出改进建议
- 如果所有指标都良好，可以进行识别测试

## 使用场景

### 场景 1：诊断采集质量问题

**问题**：采集的数据识别准确率很低

**诊断步骤**：
1. 加载一条采集数据
2. 查看波形图，确认是否有明显的信号
3. 查看能量分布，确认峰值是否清晰
4. 查看质量评分，了解信号质量
5. 查看时长统计，确认发音时长是否合理

### 场景 2：判断发音时长

**问题**：不确定实际发音时长是否超过 1 秒

**诊断步骤**：
1. 加载一条采集数据
2. 查看"原始波形"的时长信息
3. 如果显示"⚠️ 超过1秒"，表示发音时长超过 1 秒
4. 查看"时长统计"中的关键发现部分

### 场景 3：分析多峰值信号

**问题**：某些指令（如"yeah"）识别准确率低

**诊断步骤**：
1. 加载该指令的采集数据
2. 查看"能量分布"图表，确认是否有多个峰值
3. 查看"裁切参数"中的"多峰值检测"部分
4. 确认检测策略是否为"multi-peak"
5. 查看每个峰值的位置和能量

### 场景 4：对比不同采集的差异

**问题**：同一指令的不同采集差异很大

**诊断步骤**：
1. 分别加载不同采集的数据
2. 对比波形图的形状
3. 对比能量分布的峰值位置
4. 对比时长统计中的时长差异
5. 查看对比分析中的相关性和对齐得分

## 常见问题

### Q1: 波形图显示为空或全是噪声

**可能原因**：
- 采集数据质量很差
- 电极接触不良
- 采集时没有进行有效的发音

**解决方案**：
- 检查电极佩戴是否正确
- 重新采集数据，确保清晰的发音
- 查看能量分布，确认是否有有效的峰值

### Q2: 能量阈值为 0

**可能原因**：
- 采集数据全是零或非常小的值
- 数据加载出错

**解决方案**：
- 重新加载数据
- 检查采集数据是否正确保存

### Q3: 时长统计显示"超过1秒"

**含义**：
- 用户的发音时长超过 1 秒
- 可能是多音节指令或带有停顿

**建议**：
- 如果这是预期的行为，可能需要增加 FIXED_WAVEFORM_LENGTH
- 如果这不是预期的，建议重新采集，确保发音简洁

### Q4: 质量评分很低

**可能原因**：
- 信号过短（< 100 样本）
- 置信度低（< 30%）
- 未检测到峰值

**解决方案**：
- 重新采集，确保清晰的发音
- 检查电极接触
- 调整采集参数

## 最佳实践

1. **定期检查采集质量** - 在进行大量采集前，先用DEBUG页面验证几条数据
2. **对比不同采集** - 加载同一指令的多条采集，确保一致性
3. **记录关键指标** - 记下时长、质量评分等关键指标，便于后续对比
4. **监测时长变化** - 如果发现时长不一致，可能需要调整采集策略
5. **验证多峰值检测** - 对于复杂指令（如"yeah"），确认多峰值检测是否工作正常

## 技术细节

### 能量计算

能量使用滑动窗口方法计算：
```
energy[i] = sqrt(sum(waveform[i-w:i+w]²) / window_size)
```

### 阈值计算

阈值使用中位数 + 标准差方法：
```
threshold = median(energy) + 0.5 * stddev(energy)
```

### 质量评分

质量评分基于以下因素：
- 有效长度（< 100 样本 -30分，< 200 样本 -15分）
- 置信度（< 30% -25分，< 50% -10分）
- 峰值数（0 个 -20分）

### 时长计算

时长（毫秒）= (样本数 / 采样率) × 1000
- 采样率：500 Hz
- 固定长度：512 样本 = 1.024 秒

## 反馈和改进

如果在使用DEBUG页面时发现任何问题或有改进建议，请：
1. 记录具体的问题描述
2. 提供相关的采集数据
3. 附上DEBUG页面显示的关键指标
4. 提交反馈给开发团队
